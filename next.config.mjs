/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "export",
  // output: "standalone",
  // trailingSlash: true,
  reactStrictMode: true,
  swcMinify: true,
};

export default nextConfig;
